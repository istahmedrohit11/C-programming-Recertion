#include<stdio.h>
int sum_of_numbers(int n){
    int sum=0;
    while(n>0){
   sum=sum+n%10;
   n=n/10;
    }
    return sum;
}
int main(){
    int n;
    printf("Enter any number : ");
    scanf("%d",&n);
    printf("The sum is : %d ",sum_of_numbers(n));
    return 0;
}
